<div id="upload">
  <h2>Upload Image</h2>
  <form action="/upload" enctype="multipart/form-data" method="post">
    <input type="file" name="upload" />
    <input type="submit" class="green" value="Upload" />
  </form>
  <% data.forEach(function(dbData){ %>
      <img src="<%= dbData.url %>" width="100" height="100" > 
<% }); %>
</div>